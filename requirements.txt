torch
torchvision
torchmetrics
numpy
scikit-image
scikit-learn
matplotlib
tqdm
GDAL

# Used by prep-copy-tiles-from-aoi.py and prep-rgbnir-from-rgb-ir.py
# (the front of the pre-processing pipeline: discover tiles from articulation
# shapefiles / GeoPackages and fuse RGB + IR into 4-band RGBNIR rasters).
geopandas>=0.14,<1.0
rasterio>=1.3,<2.0
